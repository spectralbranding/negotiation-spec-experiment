# ONTOLOGY.yaml module - negotiation_spec_2026
#
# Per-paper ontology module per [internal path removed]
# §"ONTOLOGY.yaml schema (v0.1)". owns = terms THIS paper introduces (unique
# authority); imports = terms owned by other modules it uses (reference only).
# Linked into the substrate `terms` graph by build_ontology.py; rendered per-paper
# by render_paper_glossary.py. Bootstrapped 2026-06-13 from per-paper term
# extraction; definitions paraphrase each paper's own definitional text.

paper_key: negotiation_spec_2026
schema_version: 0.1

owns:
  - term_key: value-headroom
    label: "Value Headroom"
    canonical_form: "value headroom"
    definition: "The fraction of attainable joint value a naively cooperative pair would leave on the table, operationalized as (Pareto-joint minus naive-joint) over Pareto-joint, which moderates whether specification beats style."
    first_section: "Specification, Style, and a Result That Depends on the Exam"

imports: []

refines: []

